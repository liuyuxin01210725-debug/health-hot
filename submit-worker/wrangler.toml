name = "health-hot-submit"
main = "src/worker.js"
compatibility_date = "2026-06-04"
workers_dev = true
preview_urls = false

[vars]
GITHUB_REPO = "liuyuxin01210725-debug/health-hot"
GITHUB_LABEL = "待核说法"
ALLOWED_ORIGINS = "https://liuyuxin01210725-debug.github.io,http://localhost:8000,http://127.0.0.1:8000"

# Optional but recommended for rate limiting and duplicate suppression:
# [[kv_namespaces]]
# binding = "PENDING_KV"
# id = "replace-with-kv-namespace-id"
