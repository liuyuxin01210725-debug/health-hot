# 查过再信 · 改版决策对照（回给 Cloud Design）

> **这份文档干什么用**：你（Cloud Design）之前给了一份《改版改动清单》（9 条，参考 aihot.virxact.com）。
> 我们逐条评估了，有的做了、有的改了再做、有的先不做。下面把**你提了什么 → 我们的评估 → 最终决定 → 现在线上状态**一条条对齐，方便你再看一遍、提新意见。
>
> **角色说明**：本轮评估是 **Claude** 做的（这一轮没跑 Codex）；最终优先级是**刘宇鑫拍板**。所以下表「评估」列＝Claude 视角，「裁决」列＝用户决定。
>
> **日期**：2026-06-07 ｜ **当前内容量**：127 条核验 ｜ **线上**：https://liuyuxin01210725-debug.github.io/health-hot/

---

## 一、先纠正：清单里对 AI HOT 的两处事实错误

评估前我们真去核了 AI HOT 当前页面，发现清单的「为什么」里掺了对参考站的**错误记忆**，有两条改动的论证基础是空的（改动本身好不好另说，但理由要更正）：

1. **AI HOT 现在是「顶部导航」，不是侧边栏。** 清单说"参考它改成左侧侧边栏"——但 AI HOT 本身就是顶部导航。所以**侧边栏不是"借 AI HOT 的机制"**，是设计选择，"为了像参考站"这个理由对侧边栏不成立。
2. **AI HOT 是「浅色白底」，不是深色。** 清单说"参考站深色显科技感、我们改浅色更可信"——前提反了，AI HOT 本来就是浅色。所以"深色模式"这条的论证（参考站深色、我们要浅）是空的。

> 结论：这不影响两条改动能不能做，但说明**判断不要锚定"像不像 AI HOT"，要锚定"对一个循证健康库好不好"**。下面的评估都按后者来。

---

## 二、逐条对照（9 条）

| # | Cloud Design 提案 | Claude 评估 | 最终裁决 | 现状（2026-06-07） |
|---|---|---|---|---|
| **1** | 顶部导航 → 左侧常驻侧边栏 | 理由不成立（AI HOT 其实是顶部导航）；当前栏目还不多，别为"将要加但还没验证的栏目"先改导航。P2 不是 P0 | **P3**（最低优先） | ⬜ **未做**（刻意延后，见第四节） |
| **2** | 首页卡片网格 → 按「复核日期」分组的竖向时间轴 | **真陷阱**：把"复核日期"当主轴＝把"时效性"伪装成"权威性"，这正是健康资讯的反模式（越像新闻越不严谨）。别让日期当骨架；主轴应是主题/信任分层，日期只作次要信息 | **最终落地版**：首页「重点核验」改**横向行列表**（拿到横向扫读松弛感），**按重要性排（信任分层＋证据强度），不按时间**；复核日期降为行尾次要信息 | ✅ **已上线（横向行版）**：首页「重点核验」从方框卡片改为横向行——左＝信任徽章＋证据强度（主信号），中＝标题＋结论＋证据提示，右＝分类＋复核日期（弱化）；RCT/Meta 顶上、专家沉底，与时间无关。**另**：首页底部仍保留「最近复核」模块（两个日期都显示） |
| **3** | 每条卡片加「为什么可信/要留心」短评框（**新增 why_note 字段**） | 方向对，但**别新增字段、别为 127 条手写**：① 巨大内容债，拖慢以后每条；② 短评写歪＝凭空多一个"医学暗示"。可用**现有字段**（证据等级＋注意事项＋复核日）自动生成 | **先不做**手写 why_note；改 **P1：用现有字段生成"证据提示"** | ✅ **已上线（替代方案）**：`evidence_hint` 派生字段——用"依据状态＋证据强度"两维自动合成一句"该怎么信这条"，**零新增数据字段**；已进 claims.json（schema 1.4）＋ AI 接入页展示 |
| **4** | 配色收敛：强调色只出现在「信任信号」处 | **最该做、最被低估**——让"颜色＝可信度"形成肌肉记忆，是信息设计不是装修。P0 | **P0** | ✅ **已达成**：核查发现**设计上一版已经做到**（彩色全集中在信任徽章/证据梯度的专属变量，无装饰性滥用）。无需再改 |
| **5** | 新增可选深色模式（默认仍浅色） | 论证基础是空的（AI HOT 本就是浅色）。可做但非当前重点 | **P3**（最低优先） | ⬜ **未做**（刻意延后） |
| **6** | 求证提报**大版**：表单 ＋ 公开待查队列 ＋ 贡献者墙 ＋ N° 编号 ★清单标"最重" | **别做大版**：① 已有"搜索无结果→提交待核"**最小版**（接了 pages.dev 后端、真能创 GitHub issue）；② **公开待查队列会把未核验说法展示出来＝放大谣言风险**；③ 0 真实用户时给功能加"贡献者墙"＝给空房间装颁奖台 | **先不做**公开队列／贡献者墙／编号；**保留并打磨已有最小提交版** | ✅ **最小版在线**：搜索无结果 → 一键提交 → 进 issue（待核标签）→ 人工核验后才公开；⬜ 大版（队列/墙/编号）**不做** |
| **7** | 新增「更新日志」页（含**公开纠错**） | **这份清单里最聪明的一条**。对资讯站 changelog 只是功能列表；对核验库**公开纠错＝最强信任建设**。P0 不是 P1 | **P0** | ✅ **已上线**：`log.html`，三类标签（新增核验／重新复核／**结论更正·证据降级**），顶部承诺"会改正不可怕，藏着才可怕"；记录了真实纠错（1989→2015 换引用、BMC/BMD 措辞更正、三类信任标签降级等） |
| **8** | 新增「核验周报」页（低频改造 AI 日报） | 内容量不够、非稳定周更；**空周报比没周报更伤"持续维护"的信任**。等内容产出稳定再说 | **先不做** | ⬜ **不做**（等稳定产出） |
| **9** | 扩充「AI 接入」页（Skill / claims.json / RSS 三标签） | 已有接口，补个正式入口讲清楚，顺手该做。被 AI 调用且能区分"已核验 vs 待补"是杀手锏 | **P1** | ✅ **已上线**：`ai.html`——一键提示词（粘给 ChatGPT/Claude 即用）＋ claims.json 接口 ＋ 装 Skill（Claude Code/Codex）＋ evidence_hint 说明。⚠️ **RSS 暂未做**（claims.json 已覆盖机读需求） |

---

## 三、最终状态总览

**✅ 已做并上线（5 项）**
- 改动 4 配色收敛（核查后确认已达成）
- 改动 7 公开纠错更新日志（`log.html`）
- 改动 9 AI 接入页（`ai.html`，含一键提示词）
- 改动 3 的**替代方案**：evidence_hint 证据提示（用现有字段，不加 why_note）
- 改动 2 的**最终落地版**：首页「重点核验」改横向行列表（按重要性排、日期不当骨架）＋ 底部「最近复核」模块
- 改动 6 的**最小版**提交（早已在线）

**⬜ 刻意延后 / 不做（4 项）**
- 改动 1 侧边栏（P3，理由不成立＋栏目还不多）
- 改动 5 深色模式（P3，论证基础空）
- 改动 6 大版（公开待查队列／贡献者墙／编号——谣言放大风险＋0 用户）
- 改动 8 周报（内容产出未稳定，空周报反伤信任）

**🧱 全程不动（你自己划的边界，我们尊重）**
- 数据 schema、collect.py/build.py、发布闸门、回归测试
- 两维信任系统（信任分层 ＋ 证据强度七级）
- 纯静态、无框架、无账号、GitHub Pages
- 内容红线（非医疗建议、可回溯、标注不确定）

---

## 四、给 Cloud Design 的话 / 想请你再看的点

1. **核心分歧只有一个：改动 2 时间轴。** 你想把它做成首页**主结构**（按复核日期分组的竖向时间轴）；我们降级成了**底部一个"最近复核"模块**，卡片网格仍是首页主体。分歧根源：健康库怕"日期主轴 = 暗示越新越权威"。**如果你有办法既要时间轴的"维护感呼吸感"、又不让日期变成权威暗示，欢迎给新设计。**（用户本人现在也在重新考虑首页要不要更"横向"，所以这条是开放的。）

2. **"横向"和"日期主轴"其实是两件事。** AI HOT 的舒服感主要来自"每条是横向一行"，不一定来自"按日期分组"。如果目标是视觉更像 AI HOT，可以只把首页「重点核验」从方框卡片改成横向行列表（依据徽章仍为主信号、按重要性排、日期作次要），不必动到"日期当骨架"。**这条如果你认同，我们可以做一版给用户看。**

3. **改动 3 我们没按你的方案做，用了派生字段替代。** 如果你觉得自动生成的"证据提示"不够有"人味"、仍想要手写短评，请说明**怎么控制 127 条的内容债和医学暗示风险**——这是我们当时否掉手写版的唯一顾虑。

4. **侧边栏（改动 1）我们没否死，只是排到 P3。** 触发条件是"栏目真的多到顶部放不下"。现在导航是：精选/全部/更新/关于/AI 接入，5 项顶部够用。等栏目增加，侧边栏自然该上。

---

*附：本文件由 Claude 依据完整决策记录整理，供 Cloud Design 复看。线上可直接访问验证：首页 / log.html / ai.html。*
